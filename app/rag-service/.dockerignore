# Byte-compiled / cache
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Local cache (model weights baked in build — don't re-send from host)
.cache/

# Dev artifacts
.env
*.log
*.md
.git/
.gitignore

# Test artifacts -- tests/ IS shipped in the image so `docker compose run --rm
# rag-service pytest tests/...` works for hybrid + RRF unit tests (Phase 13).
# Only ephemeral pytest artifacts are excluded.
pytest.ini
.pytest_cache/
