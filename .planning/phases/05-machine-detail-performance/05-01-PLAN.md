---
phase: 05-machine-detail-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/backend/modules/ml/router.py
  - app/backend/modules/ml/ml_predictive.py
  - app/frontend/src/modules/shared/MachineDetailPage.tsx
  - app/frontend/src/hooks/useMachineDetail.ts
autonomous: false
requirements: []
must_haves:
  truths:
    - "Machine detail view loads in ≤ 2 s on a typical dev machine (network + render)."
    - "No duplicate HTTP request for the same machine ID occurs during a single page session."
    - "The ML prediction endpoint returns a `Cache‑Control: max‑age=300` header **and** an `ETag` that allows conditional GETs."
    - "Long lists (interventions, spare‑parts) render without jank (frame time < 16 ms)."
  artifacts:
    - path: "app/backend/modules/ml/router.py"
      provides: "ETag & conditional‑GET for ML endpoints"
    - path: "app/backend/modules/ml/ml_predictive.py"
      provides: "get_machine_detail helper"
    - path: "app/frontend/src/hooks/useMachineDetail.ts"
      provides: "Unified data‑fetch hook with client‑side cache"
    - path: "app/frontend/src/modules/shared/MachineDetailPage.tsx"
      provides: "Memoised component with virtualised lists"
---
<objective>
Eliminate duplicate loads and improve the performance of the Machine Detail view.
---
<tasks>
<task type="auto">
  <name>Task 1: Add ETag & Conditional‑GET to ML prediction endpoint</name>
  <files>app/backend/modules/ml/router.py</files>
  <action>
    Import `hashlib`. After computing prediction, generate `etag = hashlib.sha256(json.dumps(result).encode()).hexdigest()`. Compare request header `If‑None‑Match`; if matches, return `Response(status_code=304)`. Add `ETag` header to `JSONResponse`. Ensure `Cache‑Control: max‑age=300` remains.
  </action>
  <verify>
    curl -i http://localhost:8000/api/v1/ml/machines/1/prediction
    # Expect headers Cache-Control and ETag
  </verify>
</task>
<task type="auto">
  <name>Task 2: Create unified backend helper & new route</name>
  <files>app/backend/modules/ml/ml_predictive.py</files>
  <action>
    Add async function `get_machine_detail(db, machine_id)` that loads Machine and its interventions, calls `MachineLearningService.calculate_rul`, returns dict with keys `machine` and `prediction`.
    Add new FastAPI route `/api/v1/ml/machines/{machine_id}/detail` that uses the helper, re‑uses ETag/cache logic from Task 1.
  </action>
  <verify>
    python -c "from app.backend.modules.ml.ml_predictive import get_machine_detail; import asyncio; print('OK')"
  </verify>
</task>
<task type="auto">
  <name>Task 3: Add client‑side hook for unified fetch & cache</name>
  <files>app/frontend/src/hooks/useMachineDetail.ts</files>
  <action>
    Implement a React hook that fetches `/api/v1/ml/machines/{id}/detail` once, stores result in a `useRef` cache keyed by id, returns `{machine, prediction, loading, error}`. Use AbortController, handle 304 responses gracefully.
  </action>
  <verify>
    TypeScript compile passes (`npm run lint`).
  </verify>
</task>
<task type="auto">
  <name>Task 4: Refactor MachineDetailPage to use hook & virtualised lists</name>
  <files>app/frontend/src/modules/shared/MachineDetailPage.tsx</files>
  <action>
    Replace multiple fetches with `useMachineDetail`. Wrap component in `React.memo`. Memoise derived values with `useMemo`. Replace long `<ul>` renders of interventions/spare‑parts with `react-window` FixedSizeList.
  </action>
  <verify>
    Load page in browser, inspect Network – only one GET to `/detail`. Measure load time ≤ 2 s, scroll list – no jank.
  </verify>
</task>
</tasks>
<verification>
- Run frontend dev server, open Machine Detail page, capture network calls and timing.
- Ensure no duplicate GETs and that caching headers work.
</verification>
<success_criteria>
- [ ] All four tasks completed and committed.
- [ ] Page load ≤ 2 s, no duplicate API calls.
- [ ] Interventions list scrolls smoothly (< 16 ms frame).
</success_criteria>
<output>
Create `.planning/phases/05-machine-detail-performance/05-01-SUMMARY.md` after execution.
</output>
