---
phase: 03-cache
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/backend/modules/ml/ml_predictive.py
  - app/backend/modules/ml/router.py
autonomous: true
requirements:
  - OPT-05
must_haves:
  truths:
    - "In-memory TTL cache added with 5-minute expiration"
    - "calculate_rul uses cache to return cached predictions"
    - "Router adds Cache-Control header to response"
    - "Second call returns cached result (faster)"
  artifacts:
    - path: "app/backend/modules/ml/ml_predictive.py"
      provides: "TTL cache functions and integration"
      contains: "_prediction_cache"
    - path: "app/backend/modules/ml/router.py"
      provides: "Cache-Control header on prediction response"
      contains: "Cache-Control"
---

<objective>
Add lightweight in-memory TTL cache for ML prediction results.

Purpose: Cache expensive ML computations for 5 minutes to improve response times
Output: Verified cache with Cache-Control header on responses
</objective>

<context>
@.planning/phases/03-cache/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cache utility to ml_predictive.py</name>
  <files>app/backend/modules/ml/ml_predictive.py</files>
  <action>
    At TOP of ml_predictive.py (after existing imports), add:
    
    from datetime import datetime, timedelta
    from typing import Any, Optional
    
    _prediction_cache: dict[int, tuple[Any, datetime]] = {}
    CACHE_TTL_MINUTES = 5
    
    def get_cached_prediction(machine_id: int) -> Optional[dict]:
        if machine_id in _prediction_cache:
            result, timestamp = _prediction_cache[machine_id]
            if datetime.utcnow() - timestamp < timedelta(minutes=CACHE_TTL_MINUTES):
                return result
            del _prediction_cache[machine_id]
        return None
    
    def set_cached_prediction(machine_id: int, result: dict) -> None:
        _prediction_cache[machine_id] = (result, datetime.utcnow())
  </action>
  <verify>
    <automated>docker exec asset_management_backend python -c "from modules.ml.ml_predictive import get_cached_prediction, set_cached_prediction; print('OK')"</automated>
  </verify>
  <done>Cache utility functions added to ml_predictive.py</done>
</task>

<task type="auto">
  <name>Task 2: Integrate cache into calculate_rul</name>
  <files>app/backend/modules/ml/ml_predictive.py</files>
  <action>
    In calculate_rul function (around line 85), add cache check at the START:
    
    # Check cache first
    cached = get_cached_prediction(machine.id)
    if cached:
        cached["from_cache"] = True
        return cached
    
    # ... existing computation ...
    
    # Store in cache before returning
    set_cached_prediction(machine.id, response)
    return response
    
    Add "from_cache": true to cached responses to identify cache hits.
  </action>
  <verify>
    <automated>docker exec asset_management_backend python -c "from modules.ml.ml_predictive import MachineLearningService; print('OK')"</automated>
  </verify>
  <done>calculate_rul uses cache for predictions</done>
</task>

<task type="auto">
  <name>Task 3: Add Cache-Control header to router</name>
  <files>app/backend/modules/ml/router.py</files>
  <action>
    In the prediction endpoint (around line 79), wrap the return with JSONResponse:
    
    from fastapi.responses import JSONResponse
    
    return JSONResponse(
        content=prediction,
        headers={"Cache-Control": "max-age=300"}
    )
    
    This applies to: /api/v1/ml/machines/{machine_id}/prediction
  </action>
  <verify>
    <automated>docker exec asset_management_backend python -c "from modules.ml.router import router; print('OK')"</automated>
  </verify>
  <done>Router adds Cache-Control header</done>
</task>

<task type="auto">
  <name>Task 4: Verify cache is working</name>
  <files></files>
  <action>
    Test cache by calling prediction endpoint twice for same machine:
    - First call: computes and caches result
    - Second call: returns cached result (should have "from_cache": true)
    
    Compare response times - second should be faster.
  </action>
  <verify>
    <automated>docker exec asset_management_backend curl -s http://localhost:8000/api/v1/ml/machines/100/prediction | grep -o '"from_cache": true' || echo "Cache hit not found"</automated>
  </verify>
  <done>Cache verified working</done>
</task>

</tasks>

<verification>
Test cache performance:
- First call: computes prediction, stores in cache
- Second call: returns cached result (faster)
- Check for "from_cache": true in second response
</verification>

<success_criteria>
- [ ] Cache utility functions added (_prediction_cache, get_cached_prediction, set_cached_prediction)
- [ ] calculate_rul checks cache first, returns cached if available
- [ ] Router adds Cache-Control: max-age=300 header
- [ ] Second call returns cached result with from_cache: true
- [ ] Response shape unchanged (from_cache is addition)
</success_criteria>

<output>
After completion, create `.planning/phases/03-cache/03-SUMMARY.md`
</output>