---
phase: 01-backend-optimization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/backend/alembic/versions/add_performance_indexes.py
  - app/backend/alembic/versions/add_plannings_idx.py
autonomous: true
requirements:
  - OPT-02
must_haves:
  truths:
    - "9 performance indexes exist in database"
    - "Indexes cover FK columns and frequently filtered fields"
    - "Alembic migrations applied successfully"
  artifacts:
    - path: "app/backend/alembic/versions/add_performance_indexes.py"
      provides: "7 indexes on orders, interventions, notifications"
      contains: "create_index"
    - path: "app/backend/alembic/versions/add_plannings_idx.py"
      provides: "2 indexes on planning_machines"
      contains: "create_index"
---

<objective>
Create and apply Alembic migrations for database performance indexes on frequently filtered columns.

Purpose: Speed up queries that filter by FK columns and status fields
Output: Applied index migrations with verification
</objective>

<context>
@.planning/phases/01-backend-optimization/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
<parameter name>Task 1: Verify existing indexes migration</name>
<files>app/backend/alembic/versions/add_performance_indexes.py</files>
<action>
    Review the existing migration for:
    - idx_ordres_travail_machine_id
    - idx_ordres_travail_statut
    - idx_ordres_travail_utilisateur_id
    - idx_interventions_machine_id
    - idx_interventions_created_at
    - idx_notifications_lu
    - idx_notifications_utilisateur_id
    
    Ensure migration is valid and can be applied
  </action>
  <verify>
    <automated>docker exec asset_management_db psql -U postgres -d asset_management -c "SELECT indexname FROM pg_indexes WHERE indexname LIKE 'idx_%'" | wc -l</automated>
  </verify>
  <done>7 performance indexes exist</done>
</task>

<task type="auto">
<parameter name>Task 2: Create planning_machines index migration</name>
<files>app/backend/alembic/versions/add_plannings_idx.py</files>
  <action>
    Create Alembic migration with:
    - idx_planning_machines_machine_id on planning_machines(machine_id)
    - idx_planning_machines_planning_id on planning_machines(planning_id)
    
    Must depend on: new_perf_indexes (the previous migration)
  </action>
  <verify>
    <automated>docker exec asset_management_db psql -U postgres -d asset_management -c "SELECT indexname FROM pg_indexes WHERE indexname LIKE '%planning%'"</automated>
  </verify>
  <done>Indexes created on planning_machines table</done>
</task>

<task type="auto">
<name>Task 3: Apply all migrations and verify</name>
<files></files>
  <action>
    Run alembic upgrade head to apply all pending migrations
    Then verify all indexes exist in database
  </action>
  <verify>
    <automated>docker exec asset_management_db psql -U postgres -d asset_management -c "\di *idx*"</automated>
  </verify>
  <done>All migrations applied, all indexes present</done>
</task>

</tasks>

<verification>
```bash
# Check index count
docker exec asset_management_db psql -U postgres -d asset_management -c "SELECT count(*) FROM pg_indexes WHERE indexname LIKE 'idx_%'"
```
Expected: 9 indexes (7 from first migration + 2 from planning_machines)
</verification>

<success_criteria>
- [ ] add_performance_indexes migration exists and applied
- [ ] add_plannings_idx migration created and applied
- [ ] 9 performance indexes exist in database
- [ ] Indexes on correct columns (FK and filtered fields)
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-optimization/02-SUMMARY.md`
</output>