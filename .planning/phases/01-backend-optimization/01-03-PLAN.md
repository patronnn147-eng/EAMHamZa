---
phase: 01-backend-optimization
plan: 03
type: execute
wave: 2
depends_on:
  - 01-backend-optimization-01
files_modified:
  - app/backend/modules/ml/router.py
  - app/backend/tasks/maintenance_scheduler.py
autonomous: true
requirements:
  - OPT-01
must_haves:
  truths:
    - "ML fleet endpoints load data efficiently via services"
    - "Maintenance scheduler uses eager loading"
    - "API responds without N+1 query pattern"
  artifacts:
    - path: "app/backend/modules/ml/router.py"
      provides: "ML fleet endpoints with efficient queries"
      contains: "OrdresTravailService"
    - path: "app/backend/tasks/maintenance_scheduler.py"
      provides: "Maintenance scheduler task"
      contains: "service"
---

<objective>
Fix N+1 query patterns in ML router endpoints and maintenance scheduler task.

Purpose: Apply the same eager loading patterns to code that consumes the services
Output: ML endpoints and scheduler task load data efficiently
</objective>

<context>
@.planning/phases/01-backend-optimization/01-CONTEXT.md
@.planning/phases/01-backend-optimization/01-01-PLAN.md (eager loading applied to services)
</context>

<tasks>

<task type="auto">
<name>Task 1: Fix ML router fleet endpoints</name>
<files>app/backend/modules/ml/router.py</files>
  <action>
    Fix N+1 in:
    - GET /fleet/critical - iterate machines, access orders
    - GET /fleet/dashboard - iterate machines, access interventions
    
    After service changes (Plan 01), these should now work efficiently.
    Verify the code uses services that have selectinload.
  </action>
  <verify>
    <automated>docker exec asset_management_backend python -c "from modules.ml.router import router; print('OK')"</automated>
  </verify>
  <done>ML router loads machine data efficiently</done>
</task>

<task type="auto">
<name>Task 2: Fix maintenance scheduler task</name>
<files>app/backend/tasks/maintenance_scheduler.py</files>
  <action>
    Fix N+1 in preventive maintenance check:
    - When iterating over machines and accessing their orders
    
    Ensure it uses service layer with eager loading
  </action>
  <verify>
    <automated>docker exec asset_management_backend python -c "from tasks.maintenance_scheduler import check_preventive_maintenance; print('OK')"</automated>
  </verify>
  <done>Maintenance scheduler uses efficient queries</done>
</task>

<task type="auto">
<name>Task 3: Verify no N+1 in API responses</name>
 <files></files>
  <action>
    Test fleet/critical endpoint to verify related data loads without N+1
  </action>
  <verify>
    <automated>curl -s http://localhost:8000/api/v1/ml/fleet/critical | python -c "import sys,json; d=json.load(sys.stdin); print(f'Returned {len(d)} machines')"</automated>
  </verify>
  <done>API returns data without N+1 pattern</done>
</task>

</tasks>

<verification>
```bash
# Test fleet/critical endpoint
docker exec asset_management_backend curl -s http://localhost:8000/api/v1/ml/fleet/critical | head -100
```
Should return machine data with order/intervention counts efficiently
</verification>

<success_criteria>
- [ ] ML router imports services with eager loading
- [ ] Maintenance scheduler uses services properly
- [ ] API endpoints respond efficiently (no N+1 visible in timing)
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-optimization/03-SUMMARY.md`
</output>