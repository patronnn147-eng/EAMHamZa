---
phase: 04-connection-pooling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/backend/core/database.py
autonomous: true
requirements:
  - DB-POOL-01   # Ensure stable connection handling under concurrent load
must_haves:
  truths:
    - The application can handle at least 50 concurrent DB requests without exhausting connections.
    - No "cannot switch to state" errors appear under load.
    - Connection pool respects configured limits (size, overflow, timeout).
    - Lambda environments still use NullPool.
    - Non‑Lambda environments use QueuePool with the desired parameters.
  artifacts:
    - path: "app/backend/core/database.py"
      provides: "Async engine creation with pooling configuration"
      min_lines: 130
    - path: "app/backend/core/database.py"
      provides: "Documentation comments describing pool behavior"
  key_links:
    - from: "app/backend/core/database.py (engine_kwargs)"
      to: "SQLAlchemy async engine"
      via: "create_async_engine"
      pattern: "create_async_engine\(.*\)"
    - from: "Lambda detection logic"
      to: "engine_kwargs['poolclass'] = NullPool"
      via: "environment variable check"
---
<objective>
Configure SQLAlchemy async engine connection pooling to prevent connection exhaustion under concurrent load while preserving Lambda‑specific NullPool behavior.
</objective>

<execution_context>
@C:/Users/Admin/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cache/03-01-PLAN.md   # Provides example of using async_sessionmaker
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify current engine creation logic</name>
  <files>app/backend/core/database.py</files>
  <action>
    • Open `app/backend/core/database.py` and locate the `init_db` method.
    • Identify the block where `engine_kwargs` is built.
    • Ensure the existing logic already distinguishes Lambda vs non‑Lambda environments (it does).
    • Record the exact line numbers for future reference.
  </action>
  <verify>
    <automated>grep -n "engine_kwargs" -R app/backend/core/database.py</automated>
  </verify>
  <done>Engine creation block identified and line numbers documented.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor engine_kwargs to match PROMPT 5 specifications</name>
  <files>app/backend/core/database.py</files>
  <action>
    • Replace the existing `engine_kwargs` population with the exact parameters from the prompt:
      ```python
      engine_kwargs = {
          "echo": settings.debug,
          "pool_size": 10,
          "max_overflow": 20,
          "pool_timeout": 30,
          "pool_recycle": 1800,
          "pool_pre_ping": True,
      }
      ```
    • Keep the Lambda‑specific branch that injects `NullPool` **only** when `is_lambda` is true.
    • Ensure that when `is_lambda` is false we **do not** set `poolclass` at all – the QueuePool parameters above are sufficient.
    • Add a detailed docstring above the block explaining why each parameter was chosen (performance, stability, security).
    • Preserve any existing `engine_kwargs` that are not part of the prompt (e.g., future extensions) by merging with the new dict.
  </action>
  <verify>
    <automated>python - <<'PY'
import ast, pathlib, sys
src = pathlib.Path('app/backend/core/database.py').read_text()
module = ast.parse(src)
found = False
for node in ast.walk(module):
    if isinstance(node, ast.Dict):
        keys = [k.s for k in node.keys if isinstance(k, ast.Constant)]
        if set(keys) >= {"pool_size", "max_overflow", "pool_timeout", "pool_recycle", "pool_pre_ping"}:
            found = True
            break
print('FOUND' if found else 'MISSING')
PY
    </automated>
  </verify>
  <done>Engine kwargs contain the required pooling settings and respect Lambda detection.</done>
</task>

<task type="auto">
  <name>Task 3: Update imports to reflect new usage</name>
  <files>app/backend/core/database.py</files>
  <action>
    • Ensure `from sqlalchemy.pool import NullPool` remains imported (already present).
    • Verify that no obsolete imports (e.g., `QueuePool`) are lingering; remove them if present.
    • Add a comment next to the import explaining its Lambda‑only purpose.
  </action>
  <verify>
    <automated>grep -n "QueuePool" -R app/backend/core/database.py || echo "NO_QUEUEPOOL"</automated>
  </verify>
  <done>Imports are clean and correctly documented.</done>
</task>

<task type="auto">
  <name>Task 4: Add unit‑tests for pool configuration</name>
  <files>tests/test_database_pool.py</files>
  <action>
    • Create a new test module under `tests/`.
    • Write two parametrized tests:
      1. **Non‑Lambda mode** – monkey‑patch `os.environ` so `is_lambda` is False, instantiate `DatabaseManager`, call `init_db()` (awaitable). After engine creation, assert:
         - `engine.pool.size()` == 10
         - `engine.pool._max_overflow` == 20
         - `engine.pool.timeout` == 30
         - `engine.pool._recycle` == 1800
      2. **Lambda mode** – set the environment variable that triggers Lambda detection, run `init_db()`, then assert that `engine.pool.__class__.__name__` is `NullPool`.
    • Use `pytest.mark.asyncio` for async support.
    • Ensure the tests clean up by calling `await db_manager.close_db()`.
  </action>
  <verify>
    <automated>pytest -q tests/test_database_pool.py</automated>
  </verify>
  <done>Test suite validates both pooling configurations.</done>
</task>

<task type="auto">
  <name>Task 5: Verify application starts under both modes</name>
  <files>none (runtime verification)</files>
  <action>
    • Run the FastAPI server (`uvicorn main:app --reload`) with the default (non‑Lambda) environment and ensure it starts without errors.
    • Repeat with `AWS_LAMBDA_FUNCTION_NAME` set to a dummy value to simulate Lambda; confirm start-up succeeds and logs the NullPool usage.
    • Capture the logs and store them as artifacts for later reference.
  </action>
  <verify>
    <automated>bash -c "uvicorn main:app --reload & sleep 5; pkill -f uvicorn"</automated>
  </verify>
  <done>Application boots in both environments, showing correct pool selection.</done>
</task>

</tasks>

<verification>
- Run the full test suite (`pytest -q`).
- Manually start the server in both environments and curl a simple endpoint (`/health`) to ensure DB connectivity works.
- Confirm that the log output contains the messages:
  * "Using QueuePool with connection pooling for non‑Lambda environment"
  * "Using NullPool for Lambda environment to avoid connection state conflicts"
</verification>

<success_criteria>
- All unit tests pass (0 failures).
- `uvicorn` starts in both modes without raising connection‑related exceptions.
- Log statements confirming pool configuration are present.
- No new import warnings or lint errors appear (`flake8` passes).
- The codebase builds (`python -m pip install -e .`) without changes to other modules.
</success_criteria>

<output>
After successful execution, create a summary file at `.planning/phases/04-connection-pooling/04-01-SUMMARY.md` describing what was done, any open questions, and next steps (e.g., performance benchmarking under load).
</output>
