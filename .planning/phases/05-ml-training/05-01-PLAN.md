---
phase: 05-ml-training
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ml_problems/p1_failure_prediction/ml_train_basic.py
  - ml_problems/p2_failure_type/ml_train_p2.py
  - ml_problems/p3_rul_estimation/ml_train_p3.py
  - ml_problems/p4_anomaly_detection/ml_train_p4.py
  - ml_problems/p5_priority/ml_train_p5.py
  - ml_problems/p6_scheduling/ml_train_p6.py
  - app/backend/modules/ml/services/ml_retraining.py
autonomous: true
requirements: [ML-TRAIN-01, ML-TRAIN-02, ML-TRAIN-03]
user_setup: []

must_haves:
  truths:
    - "All 6 ML models retrained with improved hyperparameters"
    - "Model performance metrics are logged and better than or equal to baseline"
    - "Models saved as .pkl files in app/backend/modules/ml/models/"
    - "Retraining service updated to use improved model architecture"
  artifacts:
    - path: "app/backend/modules/ml/models/basic_machine_model.pkl"
      provides: "P1 failure prediction model"
    - path: "app/backend/modules/ml/models/ml_model_p2_failure_type.pkl"
      provides: "P2 failure type classification model"
    - path: "app/backend/modules/ml/models/ml_model_p3_rul.pkl"
      provides: "P3 RUL estimation model"
    - path: "app/backend/modules/ml/models/ml_model_p4_anomaly.pkl"
      provides: "P4 anomaly detection model"
    - path: "app/backend/modules/ml/models/ml_model_p5_priority.pkl"
      provides: "P5 priority prediction model"
    - path: "app/backend/modules/ml/models/ml_model_p6_schedule.pkl"
      provides: "P6 maintenance scheduling model"
    - path: "app/backend/modules/ml/services/ml_retraining.py"
      provides: "Updated retraining pipeline"
  key_links:
    - from: "ml_problems/*/ml_train_*.py"
      to: "app/backend/modules/ml/models/*.pkl"
      via: "joblib.dump saves trained models"
      pattern: "joblib\\.dump.*\\.pkl"
    - from: "ml_predictive.py"
      to: "app/backend/modules/ml/models/*.pkl"
      via: "joblib.load at module import"
      pattern: "joblib\\.load.*\\.pkl"
---

<objective>
Retrain all 6 ML models (P1-P6) with optimized hyperparameters, feature engineering, and cross-validation. Update the retraining service to support incremental learning.

Purpose: Improve prediction accuracy, reduce false positives, and enable continuous model improvement from production data.
Output: 6 optimized .pkl model files + updated retraining pipeline
</objective>

<execution_context>
@C:/Users/Admin/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@ai4i2020.csv
@app/backend/modules/ml/ml_predictive.py
@app/backend/modules/ml/services/ml_retraining.py
@app/backend/modules/ml/services/ml_xai.py
@ml_problems/p1_failure_prediction/ml_train_basic.py
@ml_problems/p2_failure_type/ml_train_p2.py
@ml_problems/p3_rul_estimation/ml_train_p3.py
@ml_problems/p4_anomaly_detection/ml_train_p4.py
@ml_problems/p5_priority/ml_train_p5.py
@ml_problems/p6_scheduling/ml_train_p6.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retrain P1 (failure prediction) and P2 (failure type) with XGBoost + SMOTE + GridSearchCV</name>
  <files>ml_problems/p1_failure_prediction/ml_train_basic.py, ml_problems/p2_failure_type/ml_train_p2.py</files>
  <action>
    Current state:
    - P1 uses RandomForest with basic GridSearchCV (n_estimators: 200,300; max_depth: None,10,20)
    - P2 uses MultiOutputClassifier(RandomForest) with no hyperparameter tuning
    - Both use ai4i2020.csv (10,000 rows, highly imbalanced ~3.4% failure rate)

    Upgrade P1:
    1. Switch from RandomForest to XGBoost (n_estimators=500, learning_rate=0.01, max_depth=6, subsample=0.8, colsample_bytree=0.8, scale_pos_weight for imbalance)
    2. Add SMOTE for minority class oversampling (already present, keep it)
    3. Add GridSearchCV with expanded param grid: learning_rate=[0.01, 0.05, 0.1], max_depth=[4,6,8], n_estimators=[200,500,800]
    4. Use stratified 5-fold CV with f1 scoring (not 3-fold)
    5. Add precision-recall curve analysis and AUC-PR metric
    6. Save model as dict: {'model': model, 'features': features, 'best_params': best_params, 'metrics': metrics_dict}
    7. Output must show: confusion matrix, classification report, ROC-AUC, PR-AUC

    Upgrade P2:
    1. Switch from RandomForest to XGBoost in MultiOutputClassifier
    2. Add GridSearchCV for each classifier: n_estimators=[200,500], max_depth=[4,6,8], learning_rate=[0.05,0.1]
    3. Keep temp_delta engineered feature (already present)
    4. Add feature importance analysis per label
    5. Save model with same dict format as P1
    6. Output must show per-label classification report with F1 scores

    Run both scripts from project root:
    cd C:\Users\Admin\Downloads\EAM\EAMSagemCom
    python ml_problems/p1_failure_prediction/ml_train_basic.py
    python ml_problems/p2_failure_type/ml_train_p2.py

    Verify:
    - basic_machine_model.pkl saved to app/backend/modules/ml/models/
    - ml_model_p2_failure_type.pkl saved to app/backend/modules/ml/models/
    - P1 F1 score for class 1 (failure) >= 0.85
    - P2 HDF recall >= 0.80 (most critical failure type)
  </action>
  <verify>
    <automated>python ml_problems/p1_failure_prediction/ml_train_basic.py && python ml_problems/p2_failure_type/ml_train_p2.py</automated>
  </verify>
  <done>P1 and P2 models retrained, saved as .pkl, F1 scores meet thresholds</done>
</task>

<task type="auto">
  <name>Task 2: Retrain P3 (RUL), P4 (anomaly), P5 (priority), P6 (scheduling) with optimized models</name>
  <files>ml_problems/p3_rul_estimation/ml_train_p3.py, ml_problems/p4_anomaly_detection/ml_train_p4.py, ml_problems/p5_priority/ml_train_p5.py, ml_problems/p6_scheduling/ml_train_p6.py</files>
  <action>
    Current state:
    - P3: XGBRegressor (n_estimators=500, lr=0.05, max_depth=6) — no tuning
    - P4: IsolationForest (n_estimators=100, contamination=0.04) — no tuning
    - P5: RandomForest (n_estimators=200, max_depth=12) — no tuning
    - P6: RandomForestRegressor (n_estimators=200, max_depth=15) — no tuning

    Upgrade P3 (RUL Estimation):
    1. Add GridSearchCV: n_estimators=[300,500,800], learning_rate=[0.01,0.05,0.1], max_depth=[4,6,8,10]
    2. Add feature engineering: temp_delta, rpm_torque_interaction (rpm * torque)
    3. Use MAE as primary metric, add R2 and RMSE
    4. Add residual analysis plot (predicted vs actual)

    Upgrade P4 (Anomaly Detection):
    1. Add GridSearchCV over: n_estimators=[100,200,300], contamination=[0.02,0.03,0.04,0.05], max_samples=['auto', 0.8, 0.9]
    2. Evaluate against known failure labels as proxy (precision/recall of anomaly detection)
    3. Add score distribution analysis

    Upgrade P5 (Priority):
    1. Switch to XGBoost (XGBClassifier with objective='multi:softprob', num_class=4)
    2. Add GridSearchCV: n_estimators=[200,500], max_depth=[6,10,15], learning_rate=[0.05,0.1]
    3. Add class_weight='balanced' equivalent via scale_pos_weight per class
    4. Evaluate with macro-averaged F1 score

    Upgrade P6 (Scheduling):
    1. Switch to XGBoost (XGBRegressor)
    2. Add GridSearchCV: n_estimators=[200,500], max_depth=[6,10,15], learning_rate=[0.05,0.1]
    3. Add feature engineering: temp_delta, tool_wear_squared
    4. Evaluate with MAE (days) and R2

    For all 4: save models as dict with {'model': model, 'features': features, 'best_params': params, 'metrics': metrics}
    Save to app/backend/modules/ml/models/

    Run all 4 scripts:
    cd C:\Users\Admin\Downloads\EAM\EAMSagemCom
    python ml_problems/p3_rul_estimation/ml_train_p3.py
    python ml_problems/p4_anomaly_detection/ml_train_p4.py
    python ml_problems/p5_priority/ml_train_p5.py
    python ml_problems/p6_scheduling/ml_train_p6.py
  </action>
  <verify>
    <automated>python ml_problems/p3_rul_estimation/ml_train_p3.py && python ml_problems/p4_anomaly_detection/ml_train_p4.py && python ml_problems/p5_priority/ml_train_p5.py && python ml_problems/p6_scheduling/ml_train_p6.py</automated>
  </verify>
  <done>All 4 models retrained, saved as .pkl, metrics logged and meet quality thresholds</done>
</task>

<task type="auto">
  <name>Task 3: Update retraining service and verify all models load correctly in production</name>
  <files>app/backend/modules/ml/services/ml_retraining.py, app/backend/modules/ml/ml_predictive.py</files>
  <action>
    Update ml_retraining.py:
    1. Change from XGBClassifier-only (P1) to support retraining all 6 model types
    2. Add model_type parameter: 'p1', 'p2', 'p3', 'p4', 'p5', 'p6'
    3. For P1 retraining: keep current XGBClassifier approach but add scale_pos_weight calculation from data
    4. Add retraining stats endpoint that shows last retrained timestamp, data points used, and metric improvement
    5. Add model versioning: save with timestamp suffix, keep last 3 versions
    6. Add validation step: after retraining, run test predictions and compare metrics against previous model

    Verify all models load in production:
    1. Check all 6 .pkl files exist in app/backend/modules/ml/models/
    2. Test loading each model with joblib.load
    3. Run sample predictions through ml_predictive.py calculate_rul method
    4. Verify no errors in model loading (check for [WARN] messages)

    Verify:
    - All 6 models load without errors
    - Sample prediction returns valid response
    - Retraining service supports all model types
  </action>
  <verify>
    <automated>python -c "
import sys, os
sys.path.insert(0, 'app/backend')
os.chdir('app/backend')
import joblib
models = ['basic_machine_model.pkl', 'ml_model_p2_failure_type.pkl', 'ml_model_p3_rul.pkl', 'ml_model_p4_anomaly.pkl', 'ml_model_p5_priority.pkl', 'ml_model_p6_schedule.pkl']
for m in models:
    path = os.path.join('modules', 'ml', 'models', m)
    assert os.path.exists(path), f'Missing: {path}'
    data = joblib.load(path)
    model = data['model'] if isinstance(data, dict) else data
    print(f'[OK] {m} loaded, type={type(model).__name__}')
print('All 6 models verified')
"</automated>
  </verify>
  <done>All 6 models load successfully, retraining service updated, production verification passes</done>
</task>

</tasks>

<verification>
- All 6 .pkl model files exist in app/backend/modules/ml/models/
- Each model loads without errors
- P1 F1 (failure class) >= 0.85
- P2 HDF recall >= 0.80
- P3 R2 >= 0.70
- P4 anomaly detection precision >= 0.70
- P5 macro F1 >= 0.80
- P6 R2 >= 0.70
- Retraining service supports all 6 model types
- Backend container starts without model loading errors
</verification>

<success_criteria>
- All 6 ML models retrained with optimized hyperparameters and improved metrics
- Model files saved in correct location for production loading
- Retraining pipeline updated to support incremental learning for all models
- Backend loads all models without warnings
- Prediction accuracy improved or maintained vs baseline
</success_criteria>

<output>
After completion, create `.planning/phases/05-ml-training/05-01-SUMMARY.md` with:
- Model metrics table (before/after comparison)
- Any issues encountered during training
- Recommendations for future model improvements
</output>
