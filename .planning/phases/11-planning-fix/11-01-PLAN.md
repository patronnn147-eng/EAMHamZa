---
phase: 11-planning-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/backend/services/plannings.py
  - app/backend/models/plannings.py
autonomous: true
requirements:
  - PF-01
user_setup: []
must_haves:
  truths:
    - "Backend API returns assigned_users (technicians) and machine_ids for each planning record."
  artifacts:
    - path: "app/backend/services/plannings.py"
      provides: "eager‑loaded query for Plannings with users and machines"
    - path: "app/backend/models/plannings.py"
      provides: "relationship definitions for planning_utilisateurs and planning_machines"
  key_links:
    - from: "PlanningsService.get_by_id"
      to: "SQLAlchemy relationship eager loading"
      via: "selectinload"
      pattern: "selectinload.*planning_utilisateurs|planning_machines"
---

<objective>
Add eager loading to the Plannings service so that the API includes assigned technicians and selected machines.

Purpose: Fix the Planning page bug where these related entities were missing, improving UI correctness and performance.
Output: Updated service and model files, migrations (if needed) committed.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add eager‑loading to PlanningsService.get_by_id</name>
  <files>app/backend/services/plannings.py</files>
  <action>Import ``selectinload`` and modify the ``query`` in ``get_by_id`` to include ``selectinload(Plannings.planning_utilisateurs).selectinload(Planning_utilisateurs.utilisateur)`` and ``selectinload(Plannings.planning_machines)`` so that related users and machines are fetched in a single query.</action>
  <verify><automated>pytest -q tests/backend/test_plannings_service.py::test_get_by_id_eager_loading -x</automated></verify>
  <done>API response JSON contains ``assigned_users`` array with technician objects and ``machine_ids`` array for the requested planning ID.</done>
</task>
</tasks>

<verification>
Run the backend test suite and manually query the API endpoint ``/api/plannings/{id}`` to confirm the related data appears.
</verification>

<success_criteria>
All unit and integration tests pass, and a manual curl request shows ``assigned_users`` and ``machine_ids`` fields.
</success_criteria>

<output>
After completion, create ``.planning/phases/11-planning-fix/11-01-SUMMARY.md`` summarizing the changes.
</output>
