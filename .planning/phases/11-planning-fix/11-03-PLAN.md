---
phase: 11-planning-fix
plan: 03
type: execute
wave: 3
depends_on:
  - 11-02
files_modified:
  - tests/backend/test_plannings_service.py
  - tests/frontend/planning_ui.test.tsx
autonomous: true
requirements:
  - PF-03
user_setup: []
must_haves:
  truths:
    - "Regression tests verify that the backend eager‑loading and frontend UI hydration stay correct after future changes."
  artifacts:
    - path: "tests/backend/test_plannings_service.py"
      provides: "backend integration test for eager‑loading"
    - path: "tests/frontend/planning_ui.test.tsx"
      provides: "frontend UI test for form hydration"
  key_links:
    - from: "backend test"
      to: "backend eager‑loading implementation"
      via: "assertion on response JSON"
    - from: "frontend UI test"
      to: "frontend hydration code"
      via: "DOM query for selected options"
---

<objective>
Add regression tests for the Planning page bug fix, covering both backend eager‑loading and frontend UI hydration.

Purpose: Guard against future regressions by ensuring the assigned technicians and machine selections remain correctly fetched and displayed.
Output: New test files committed and passing.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Backend integration test for eager‑loading</name>
  <files>tests/backend/test_plannings_service.py</files>
  <action>Create a pytest that inserts a planning record with related ``Planning_utilisateurs`` and ``Planning_machines`` rows, calls ``PlanningsService.get_by_id`` and asserts that the returned object contains non‑empty ``assigned_users`` and ``machine_ids`` collections.
</action>
  <verify><automated>pytest -q tests/backend/test_plannings_service.py::test_eager_loading -x</automated></verify>
  <done>Test passes, confirming the service returns the related data.</done>
</task>
<task type="auto">
  <name>Task 2: Frontend UI test for form hydration</name>
  <files>tests/frontend/planning_ui.test.tsx</files>
  <action>Write a Playwright (or React Testing Library) test that loads the Planning Management page for an existing planning entry, verifies that the multi‑select for technicians contains the expected pre‑selected options and that the machine dropdown shows the saved machine ids.
</action>
  <verify><automated>npm run test -- -t "planning_ui"</automated></verify>
  <done>Both UI checks pass without errors.
</done>
</task>
</tasks>

<verification>
Run the full test suite (`npm test && pytest`) and confirm zero failures.
</verification>

<success_criteria>
All new tests pass and the existing test suite remains green.
</success_criteria>

<output>
After completion, create ``.planning/phases/11-planning-fix/11-03-SUMMARY.md`` summarizing the added tests.
</output>
