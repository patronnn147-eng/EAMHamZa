---
phase: 11-planning-fix
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - app/frontend/src/modules/admin/PlanningManagement.tsx
  - app/frontend/src/modules/shared/PlanningCalendarView.tsx
autonomous: true
requirements:
  - PF-02
user_setup: []
must_haves:
  truths:
    - "Frontend forms for planning show assigned technicians and selected machines correctly."
  artifacts:
    - path: "app/frontend/src/modules/admin/PlanningManagement.tsx"
      provides: "UI hydration logic for assigned users and machines"
    - path: "app/frontend/src/modules/shared/PlanningCalendarView.tsx"
      provides: "Calendar view integrates planning data"
  key_links:
    - from: "PlanningManagement component"
      to: "backend GET /api/plannings/{id}"
      via: "fetch call and state update"
      pattern: "fetch.*plannings.*assigned_users|machine_ids"
---

<objective>
Add frontend logic to hydrate the Planning Management UI with the assigned technicians and selected machines returned by the backend.

Purpose: Complete the Planning page bug by ensuring the edit/create forms pre‑populate the correct technicians and machine selections.
Output: Updated TypeScript/React components, type definitions, and UI behavior.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Update PlanningManagement component to fetch and display assigned_users and machine_ids</name>
  <files>app/frontend/src/modules/admin/PlanningManagement.tsx</files>
  <action>Modify the useEffect / data‑loading hook to include ``assigned_users`` and ``machine_ids`` from the API response, map them to the existing multi‑select and dropdown UI components, and ensure the component state reflects these values for edit mode.</action>
  <verify><automated>npm run test -- -t "PlanningManagement"</automated></verify>
  <done>When opening an existing planning entry, the form shows the correct list of technicians pre‑selected and the machine dropdown reflects the saved machine ids.</done>
</task>
<task type="auto">
  <name>Task 2: Adjust PlanningCalendarView to pass planning data downstream</name>
  <files>app/frontend/src/modules/shared/PlanningCalendarView.tsx</files>
  <action>Ensure the calendar component propagates the full planning object (including ``assigned_users`` and ``machine_ids``) to the edit modal, updating its props interface accordingly.</action>
  <verify><automated>npm run test -- -t "PlanningCalendarView"</automated></verify>
  <done>The calendar’s edit modal opens with the correct technicians and machine selections populated.</done>
</task>
</tasks>

<verification>
Run the full frontend test suite and manually verify that the Planning Management UI displays the correct assigned technicians and machine selections for an existing planning entry.
</verification>

<success_criteria>
All unit tests pass, and a manual UI check confirms the form hydration works without errors.
</success_criteria>

<output>
After completion, create ``.planning/phases/11-planning-fix/11-02-SUMMARY.md`` summarizing the UI changes.
</output>
