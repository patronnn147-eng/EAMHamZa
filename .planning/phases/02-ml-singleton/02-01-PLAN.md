---
phase: 02-ml-singleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/backend/modules/ml/ml_predictive.py
  - app/backend/modules/ml/router.py
autonomous: true
requirements:
  - OPT-04
must_haves:
  truths:
    - "get_model() singleton loads .pkl once at module init"
    - "Router handles model_unavailable when get_model() returns None"
    - "Second API call faster than first (singleton verified)"
  artifacts:
    - path: "app/backend/modules/ml/ml_predictive.py"
      provides: "Singleton get_model() function"
      contains: "def get_model"
    - path: "app/backend/modules/ml/router.py"
      provides: "API endpoints with model_unavailable fallback"
      contains: "model_unavailable"
---

<objective>
Ensure ML model is loaded once via singleton pattern and handle fallback when model unavailable.

Purpose: Performance optimization - load .pkl once instead of on every request
Output: Verified singleton behavior with fallback handling
</objective>

<context>
@.planning/phases/02-ml-singleton/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify singleton pattern exists</name>
  <files>app/backend/modules/ml/ml_predictive.py</files>
  <action>
    Verify that:
    1. Module-level _MODEL = None exists (line 18)
    2. get_model() function exists with lazy loading (lines 21-26)
    3. get_model() is called instead of joblib.load() in functions
    
    The singleton pattern is already implemented - verify it's working.
  </action>
  <verify>
    <automated>docker exec asset_management_backend python -c "from modules.ml.ml_predictive import get_model; m = get_model(); print('Model loaded:', type(m))"</automated>
  </verify>
  <done>Singleton pattern verified in ml_predictive.py</done>
</task>

<task type="auto">
  <name>Task 2: Add model_unavailable fallback to router</name>
  <files>app/backend/modules/ml/router.py</files>
  <action>
    Find where calculate_rul is called and add model_unavailable: true to response when model unavailable.
    
    The get_model() returns None when:
    - Model file doesn't exist at basic_machine_model.pkl
    - Model fails to load
    
    Add "model_unavailable": True to the prediction response when model cannot be used.
  </action>
  <verify>
    <automated>docker exec asset_management_backend python -c "from modules.ml.router import router; print('OK')"</automated>
  </verify>
  <done>Router handles model_unavailable case</done>
</task>

<task type="auto">
  <name>Task 3: Verify performance improvement</name>
  <files></files>
  <action>
    Call GET /api/v1/ml/machines/1/prediction twice:
    - First call loads model (singleton initialization)
    - Second call should be faster (model already in memory)
    
    Compare timing of both calls.
  </action>
  <verify>
    <automated>docker exec asset_management_backend curl -s -w "\nTime: %{time_total}s\n" http://localhost:8000/api/v1/ml/machines/1/prediction | tail -2</automated>
  </verify>
  <done>Second call faster than first</done>
</task>

</tasks>

<verification>
Test singleton - call twice and compare times:
- First call: loads model (singleton initialization)
- Second call: should be faster (model cached in memory)
</verification>

<success_criteria>
- [ ] get_model() singleton exists and works
- [ ] Router adds model_unavailable when model None
- [ ] Second API call faster than first (performance verified)
- [ ] API response shape unchanged (except model_unavailable field)
</success_criteria>

<output>
After completion, create `.planning/phases/02-ml-singleton/01-SUMMARY.md`
</output>