---
phase: 09-ml-pipeline-rebuild
plan: 04
type: execute
wave: 1
depends_on: [09-01, 09-02, 09-03]
files_modified: []
autonomous: true
requirements: [ML-NEW-04]

must_haves:
  truths:
    - "Drift detection working"
    - "Monitoring endpoints available"
    - "Alerts configured"
  artifacts:
    - path: "app/ml-microservice/src/drift_detector.py"
      provides: "Drift detection"
  key_links:
    - from: "src/router.py"
      to: "src/drift_detector.py"
      via: "import"
---

<objective>
Add drift detection and monitoring following senior-ml-engineer skill.

Following MLOps patterns:
- Statistical drift detection (KS test)
- Prediction logging
- Monitoring endpoints

Purpose: Detect when model data distribution changes.
</objective>

<execution_context>
@C:/Users/Admin/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@app/ml-microservice/src/router.py (existing)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add drift detection endpoints</name>
  <files>app/ml-microservice/src/router.py</files>
  <action>
Add to router.py:

```python
# Add to imports
from .drift_detector import drift_detector, check_drift, update_baseline, get_drift_status

# Add endpoints
@router.post("/drift/update-baseline")
async def update_drift_baseline(data: TelemetryInput):
    """Update drift detection baseline."""
    features = [
        data.air_temperature, data.process_temperature,
        data.rotational_speed, data.torque, data.tool_wear
    ]
    update_baseline(features)
    return {"success": True, "message": "Baseline updated"}

@router.get("/drift/status")
async def drift_status():
    """Get drift detection status."""
    return get_drift_status()

@router.get("/drift/check")
async def drift_check():
    """Check current drift status."""
    return drift_detector.detect_drift()

@router.post("/drift/reset")
async def reset_drift():
    """Reset drift detection."""
    drift_detector.reset_current()
    return {"success": True, "message": "Current window reset"}
```
  </action>
  <verify>Endpoints exist</verify>
  <done>Drift endpoints added</done>
</task>

<task type="auto">
  <name>Task 2: Add prediction logging</name>
  <files>app/ml-microservice/src/router.py</files>
  <action>
Add prediction logging to track all predictions:

```python
import logging
from datetime import datetime

# Add prediction logger
prediction_log = []

@router.get("/logs/predictions")
async def get_prediction_logs(limit: int = 100):
    """Get recent prediction logs."""
    return {
        "logs": prediction_log[-limit:],
        "total": len(prediction_log)
    }

# Modify predict-all to log
@router.post("/predict-all")
async def predict_all(data: TelemetryInput):
    # ... existing code ...
    
    # Log prediction
    prediction_log.append({
        "timestamp": datetime.now().isoformat(),
        "input": {...},
        "prediction": pred
    })
    
    # Keep only last 10000
    if len(prediction_log) > 10000:
        prediction_log[:] = prediction_log[-10000:]
    
    return {...}
```
  </action>
  <verify>Logging works</verify>
  <done>Prediction logging added</done>
</task>

</tasks>

<verification>
[ ] Drift baseline can be updated
[ ] Drift status endpoint returns status
[ ] Predictions are logged
[ ] /logs/predictions works
</verification>

<success_criteria>
Drift detection and monitoring working.

Measurable:
- /drift/status shows baseline count
- /logs/predictions returns recent predictions
</success_criteria>

<output>
After completion, create `.planning/phases\09-ml-pipeline-rebuild\09-04-SUMMARY.md`
</output>
