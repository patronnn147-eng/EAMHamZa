---
phase: 06-ml-dashboard
plan: 01
type: execute
wave: 1
depends_on: [05-ml-training]
files_modified:
  - app/frontend/src/modules/shared/MLFleetDashboard.tsx
  - app/frontend/src/modules/shared/ml-fleet-dashboard/
  - app/frontend/src/modules/shared/machines/components/MachineHealthPanel.tsx
  - app/frontend/src/modules/shared/machines/components/PredictivePanel.tsx
  - app/frontend/src/lib/types.ts
  - app/frontend/src/app/routing/AppRoutes.tsx
  - app/frontend/src/lib/api.ts
autonomous: true
requirements: [ML-DASH-01, ML-DASH-02, ML-DASH-03]
user_setup: []

must_haves:
  truths:
    - "ML Fleet Dashboard page accessible at /ml-dashboard route"
    - "Dashboard displays fleet-wide health overview with color-coded machine cards"
    - "Machine detail panel shows RUL, failure probability, health score, reliability score"
    - "SHAP explanations displayed as top contributing factors"
    - "Trend charts show health score and RUL over time"
    - "Anomaly alerts displayed for flagged machines"
    - "All components use existing Shadcn/UI + Tailwind styling patterns"
    - "TestSprite tests pass for all dashboard endpoints and UI interactions"
  artifacts:
    - path: "app/frontend/src/modules/shared/MLFleetDashboard.tsx"
      provides: "Main ML Fleet Dashboard page component"
    - path: "app/frontend/src/modules/shared/ml-fleet-dashboard/hooks/useMLFleetData.ts"
      provides: "Data fetching hook for ML predictions"
    - path: "app/frontend/src/modules/shared/ml-fleet-dashboard/components/FleetOverview.tsx"
      provides: "Fleet-wide summary cards and machine grid"
    - path: "app/frontend/src/modules/shared/ml-fleet-dashboard/components/MachineDetailPanel.tsx"
      provides: "Detailed ML prediction panel for selected machine"
    - path: "app/frontend/src/modules/shared/ml-fleet-dashboard/components/HealthTrendChart.tsx"
      provides: "Recharts-based health score trend visualization"
    - path: "app/frontend/src/modules/shared/ml-fleet-dashboard/components/AnomalyAlerts.tsx"
      provides: "Anomaly detection alerts component"
    - path: "app/frontend/src/modules/shared/ml-fleet-dashboard/components/SHAPExplanations.tsx"
      provides: "SHAP feature importance visualization"
  key_links:
    - from: "MLFleetDashboard.tsx"
      to: "/api/v1/ml/fleet/dashboard"
      via: "fetch() with Bearer token"
      pattern: "fetch.*ml/fleet/dashboard"
    - from: "MachineDetailPanel.tsx"
      to: "/api/v1/ml/machines/{id}/prediction"
      via: "fetch() with Bearer token"
      pattern: "fetch.*ml/machines.*prediction"
---

<objective>
Build a comprehensive ML Fleet Dashboard in the React frontend that visualizes all 6 ML model predictions (P1-P6) for the entire machine fleet. Includes fleet overview, machine detail panels, trend charts, SHAP explanations, and anomaly alerts.

Purpose: Translate backend ML predictions into actionable insights for CHETOP, CHEFTECH, and ADMIN users.
Output: New /ml-dashboard route with full ML visualization suite + TestSprite tests
</objective>

<execution_context>
@C:/Users/Admin/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/frontend/src/modules/shared/MachineDetailPage.tsx
@app/frontend/src/modules/shared/machines/components/MachineHealthPanel.tsx
@app/frontend/src/modules/shared/machines/components/PredictivePanel.tsx
@app/frontend/src/modules/shared/ReliabilityDashboardTab.tsx
@app/frontend/src/modules/admin/SystemAnalytics.tsx
@app/frontend/src/modules/admin/ml/MLDashboard.tsx
@app/frontend/src/lib/types.ts
@app/frontend/src/lib/api.ts
@app/frontend/src/lib/utils.ts
@app/frontend/src/app/routing/AppRoutes.tsx
@app/frontend/src/index.css
@app/frontend/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ML Fleet Dashboard page with fleet overview and machine grid</name>
  <files>app/frontend/src/modules/shared/MLFleetDashboard.tsx, app/frontend/src/modules/shared/ml-fleet-dashboard/hooks/useMLFleetData.ts, app/frontend/src/modules/shared/ml-fleet-dashboard/components/FleetOverview.tsx</files>
  <action>
    1. Create MLFleetDashboard.tsx as main page component following existing patterns:
       - Uses Layout wrapper (same as SystemAnalytics, AdminMachines)
       - Custom hook useMLFleetData() for data fetching
       - Loading state with LoadingSpinner
       - Error state with toast notification

    2. Create useMLFleetData.ts hook:
       - Fetches /api/v1/ml/fleet/dashboard for all machines with ML scores
       - Fetches /api/v1/ml/fleet/critical for high-risk machines
       - Returns: { machines, criticalMachines, loading, error, refetch }
       - Uses fetch() with Bearer token (same pattern as SystemAnalytics)
       - Subscribes to DataSyncContext for machine updates

    3. Create FleetOverview.tsx component:
       - Summary cards: Total Machines, Critical Risk, High Risk, Average Health Score
       - Color-coded machine grid using existing MachinesGrid pattern
       - Each card shows: machine name, zone, health score bar, risk level badge, RUL
       - Click machine → opens MachineDetailPanel
       - Filter by risk level (All, Critical, High, Medium, Low)
       - Sort by health score, RUL, or risk level

    4. Add route in AppRoutes.tsx:
       - /ml-dashboard → MLFleetDashboard (ADMIN, CHEFTECH, CHETOP)
       - Add sidebar nav link in Layout.tsx for ADMIN and CHEFTECH roles

    5. Add types to types.ts:
       - MLPrediction interface matching backend response
       - FleetDashboard interface

    Run: cd app/frontend && pnpm dev (verify page loads)
    Verify: Dashboard loads with fleet data, cards display correctly, filtering works
  </action>
  <verify>
    <automated>cd app/frontend && pnpm build</automated>
  </verify>
  <done>ML Fleet Dashboard page created with fleet overview, machine grid, filtering, and routing</done>
</task>

<task type="auto">
  <name>Task 2: Create Machine Detail Panel with ML predictions, SHAP explanations, and trend charts</name>
  <files>app/frontend/src/modules/shared/ml-fleet-dashboard/components/MachineDetailPanel.tsx, app/frontend/src/modules/shared/ml-fleet-dashboard/components/HealthTrendChart.tsx, app/frontend/src/modules/shared/ml-fleet-dashboard/components/SHAPExplanations.tsx, app/frontend/src/modules/shared/ml-fleet-dashboard/components/AnomalyAlerts.tsx</files>
  <action>
    1. Create MachineDetailPanel.tsx:
       - Dialog/Sheet component (use Shadcn Dialog or Sheet)
       - Fetches /api/v1/ml/machines/{id}/prediction for detailed ML data
       - Displays:
         * Machine name, zone, status
         * RUL (Remaining Useful Life) with gauge/progress bar
         * Failure Probability (0-100%) with color-coded bar
         * Health Score (0-100) with breakdown
         * Reliability Score (0-100)
         * Risk Level badge (CRITICAL/HIGH/MEDIUM/LOW)
         * Predicted Priority (Critical/High/Medium/Normal)
         * Failure Type predictions (TWF, HDF, PWF, OSF, RNF)
         * SHAP Explanations (top 3 contributing factors)
         * Anomaly detection status
       - Action buttons: "Create Work Order", "Schedule Maintenance"

    2. Create HealthTrendChart.tsx:
       - Recharts LineChart showing health score trend over time
       - Dual axis: health score (line) + RUL (area)
       - Uses existing Recharts patterns from SystemAnalytics
       - Responsive container with tooltip

    3. Create SHAPExplanations.tsx:
       - Bar chart showing top contributing factors to risk
       - Color-coded by intensity (red=high risk factor, green=low)
       - Uses Shadcn Card + Recharts BarChart
       - Shows factor name, contribution value, intensity level

    4. Create AnomalyAlerts.tsx:
       - Alert banner when machine is flagged as anomalous
       - Shows anomaly score and detection confidence
       - Uses Shadcn Alert component
       - Links to anomaly details

    5. Integrate all components into MachineDetailPanel
    6. Connect to FleetOverview click handler

    Run: cd app/frontend && pnpm build
    Verify: Clicking a machine opens detail panel with all ML data, charts render correctly
  </action>
  <verify>
    <automated>cd app/frontend && pnpm build</automated>
  </verify>
  <done>Machine Detail Panel with full ML predictions, SHAP explanations, trend charts, and anomaly alerts</done>
</task>

<task type="auto">
  <name>Task 3: TestSprite testing + polish + commit</name>
  <files>app/frontend/src/modules/shared/MLFleetDashboard.tsx, app/frontend/src/modules/shared/ml-fleet-dashboard/</files>
  <action>
    1. Run TestSprite frontend tests against the ML Dashboard:
       - Bootstrap TestSprite with frontend mode, port 5173 (Vite dev server)
       - Test plan should include:
         * Dashboard loads and displays fleet overview
         * Machine cards show correct health scores and risk levels
         * Filtering by risk level works
         * Clicking a machine opens detail panel
         * Detail panel shows ML predictions
         * Charts render correctly
         * Anomaly alerts display when applicable

    2. Fix any TestSprite failures by updating components or API responses

    3. Run full build: cd app/frontend && pnpm build

    4. Verify backend still serves ML endpoints correctly

    5. Commit all changes

    Verify: TestSprite tests pass, build succeeds, all features work
  </action>
  <verify>
    <automated>cd app/frontend && pnpm build</automated>
  </verify>
  <done>All TestSprite tests pass, build succeeds, ML Dashboard fully functional</done>
</task>

</tasks>

<verification>
- ML Fleet Dashboard accessible at /ml-dashboard route
- Dashboard displays fleet-wide overview with summary cards
- Machine grid shows color-coded health status
- Filtering and sorting work correctly
- Machine detail panel opens with full ML predictions
- SHAP explanations displayed as bar chart
- Health trend chart renders with Recharts
- Anomaly alerts display when machine is flagged
- TestSprite frontend tests pass
- pnpm build succeeds with no errors
- Backend ML endpoints return correct data
</verification>

<success_criteria>
- ML Fleet Dashboard page fully functional with all 6 model predictions visualized
- Machine detail panel shows comprehensive ML insights
- All charts and visualizations render correctly
- TestSprite tests pass for dashboard functionality
- Build succeeds with no TypeScript or lint errors
- Dashboard accessible to ADMIN, CHEFTECH, and CHETOP roles
</success_criteria>

<output>
After completion, create `.planning/phases/06-ml-dashboard/06-01-SUMMARY.md` with:
- Features implemented
- TestSprite test results
- Screenshots/descriptions of dashboard components
- Any issues encountered and resolutions
- Recommendations for future enhancements
</output>
