---
phase: 08-intelligence-automation
plan: 05
type: execute
wave: 2
depends_on: []
files_modified: []
autonomous: true
requirements: [INTEL-07]
user_setup: []

must_haves:
  truths:
    - "Every change to asset data is logged with who, what, when"
    - "User can view complete history for any machine"
    - "Audit logs are append-only (no modification/deletion)"
  artifacts:
    - path: "app/backend/services/audit.py"
      provides: "Audit logging service"
      exports: ["log_create", "log_update", "log_delete", "get_audit_log"]
    - path: "app/backend/core/audit_events.py"
      provides: "SQLAlchemy event listeners for automatic audit"
      exports: ["setup_audit_listeners"]
    - path: "app/frontend/src/modules/shared/AuditLogViewer.tsx"
      provides: "UI for viewing audit history"
      exports: ["AuditLogViewer", "MachineHistoryPanel"]
  key_links:
    - from: "app/backend/core/audit_events.py"
      to: "app/backend/services/archives.py"
      via: "write audit entries to archives table"
      pattern: "archives.*create"
    - from: "app/frontend/src/modules/shared/AuditLogViewer.tsx"
      to: "app/backend/modules/shared/archives.py"
      via: "fetch audit history"
      pattern: "GET.*archives.*machine"
---

<objective>
Implement Audit Trail/History Log for tracking all asset changes.
</objective>

<context>
@.planning/phases/08-intelligence-automation/08-CONTEXT.md
@app/backend/services/archives.py
@app/backend/modules/shared/archives.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance Archives Table and Service</name>
  <files>app/backend/services/audit.py</files>
  <action>
Extend existing archives table schema:
- Add action_type: enum (CREATE, UPDATE, DELETE, VIEW)
- Add entity_type: string (machine, work_order, intervention, etc.)
- Add entity_id: UUID
- Add user_id: FK to utilisateurs
- Add user_name: string (denormalized for when user is deleted)
- Add changes: JSON (before/after values for UPDATE)
- Add ip_address: string
- Make existing fields more descriptive

Create audit service:
- `log_audit(entity_type, entity_id, action_type, user_id, changes, ip_address)`
- `get_audit_log(entity_type, entity_id, from_date, to_date, user_id, page, limit)`
- `get_audit_stats(entity_type, entity_id)` — count by action_type, user
  </action>
  <verify>
<automated>python -c "from app.backend.services.audit import log_audit; print('OK')"</automated>
  </verify>
  <done>Audit service created and importable</done>
</task>

<task type="auto">
  <name>Task 2: Create SQLAlchemy Audit Event Listeners</name>
<files>app/backend/core/audit_events.py</files>
<action>
Create automatic audit logging using SQLAlchemy events:

Setup function `setup_audit_listeners()`:
- `before_flush` listener to track all inserts/updates/deletes
- Capture old values (before_update) and new values
- Queue audit entries for batch insert (performance)
- Handle relationships (e.g., when machine status changes)

Apply to key models:
- Machine
- WorkOrder (OrdreTravail)
- Intervention (OrdreIntervention)
- Planning
- User

Note: Append-only — deletes should not actually delete, but mark as deleted.
  </action>
<verify>
<automated>python -c "from app.backend.core.audit_events import setup_audit_listeners; print('OK')"</automated>
  </verify>
  <done>Audit listeners registered on startup</done>
</task>

<task type="auto">
  <name>Task 3: Create Audit Log API</name>
<files>app/backend/modules/shared/audit.py</files>
<action>
Create audit API endpoints:
- GET /api/v1/audit/log — List audit entries (paginated)
  - Filters: entity_type, entity_id, user_id, action_type, date_range
- GET /api/v1/audit/log/{entity_type}/{entity_id} — History for specific entity
- GET /api/v1/audit/stats — Summary statistics
- GET /api/v1/audit/export — Export to CSV

Secure with role-based access (admin sees all, others see limited).
  </action>
<verify>
<automated>curl -X GET "http://localhost:8000/api/v1/audit/stats" -H "Authorization: Bearer $TOKEN"</automated>
  </verify>
  <done>Audit API returns history data</done>
</task>

<task type="auto">
  <name>Task 4: Create Audit Log UI</name>
<files>app/frontend/src/modules/shared/AuditLogViewer.tsx</files>
<action>
Create audit log viewer components:

Global Audit Log Page (Admin):
- Table of all changes
- Filters: user, entity type, date range
- Click to view details (before/after)
- Export to CSV

Machine History Panel:
- Timeline view of changes to specific machine
- Show who made change, what changed, when
- Diff view for updates (before → after)
- Integrated into MachineDetail page

Use shadcn/ui components (Table, Timeline, Card).
  </action>
<verify>
<automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Audit log UI renders without errors</done>
</task>

</tasks>

<verification>
- Every machine update creates audit entry with user and changes
- User can view history of changes for any machine
- Admin can export full audit log
</verification>

<success_criteria>
- All data modifications tracked with user attribution
- Complete history viewable for any asset
- Audit logs are immutable (append-only)
</success_criteria>

<output>
After completion, create `.planning/phases/08-intelligence-automation/08-05-SUMMARY.md`
</output>
