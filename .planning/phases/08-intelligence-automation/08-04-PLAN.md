---
phase: 08-intelligence-automation
plan: 04
type: execute
wave: 2
depends_on: [08-01]
files_modified: []
autonomous: true
requirements: [INTEL-06]
user_setup: []

must_haves:
  truths:
    - "User can view real-time telemetry data for each machine"
    - "User can see telemetry trends over time (charts)"
    - "Dashboard shows IoT health status for all machines"
  artifacts:
    - path: "app/frontend/src/modules/shared/IoTDashboard.tsx"
      provides: "IoT fleet overview dashboard"
      exports: ["IoTDashboard"]
    - path: "app/frontend/src/modules/shared/TelemetryPanel.tsx"
      provides: "Machine telemetry detail view"
      exports: ["TelemetryPanel"]
    - path: "app/frontend/src/components/telemetry/TrendChart.tsx"
      provides: "Time-series charts for telemetry"
      exports: ["TrendChart"]
  key_links:
    - from: "app/frontend/src/modules/shared/IoTDashboard.tsx"
      to: "app/backend/modules/ml/router.py"
      via: "fetch telemetry data"
      pattern: "GET.*telemetry"
    - from: "app/frontend/src/components/telemetry/TrendChart.tsx"
      to: "recharts library"
      via: "render time-series"
      pattern: "LineChart.*recharts"
---

<objective>
Implement IoT/Sensor Integration Dashboard showing telemetry trends and real-time machine health.
</objective>

<context>
@.planning/phases/08-intelligence-automation/08-CONTEXT.md
@app/backend/services/machines.py
@app/frontend/src/modules/shared
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create IoT Fleet Dashboard</name>
  <files>app/frontend/src/modules/shared/IoTDashboard.tsx</files>
<action>
Create IoT Dashboard page:
- Grid of machine cards showing:
  - Machine name and status
  - Current telemetry values (temp, vibration, RPM, torque)
  - Health indicator (green/yellow/red based on thresholds)
  - Last updated timestamp
- Summary stats: Total machines, Online, Warning, Critical
- Filter by zone, status, health
- Auto-refresh every 30 seconds (polling)

Use existing machine card components, add telemetry overlay.
  </action>
<verify>
<automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>IoT dashboard renders with machine telemetry</done>
</task>

<task type="auto">
  <name>Task 2: Create Telemetry Detail Panel</name>
<parameter name="files>app/frontend/src/modules/shared/TelemetryPanel.tsx</files>
<action>
Create machine telemetry panel:
- Current values display (large numbers with units)
- Min/Max/Average statistics
- Threshold indicators (warning/critical ranges)
- Historical chart (last 7 days, 30 days, 90 days)
- Anomaly highlights (points outside normal range highlighted)
- Export data button (CSV)

Integrate with existing MachineDetail components.
  </action>
<verify>
<staticmethod>npm run build 2>&1 | tail -20</staticmethod>
  </verify>
  <done>Telemetry panel displays historical data</done>
</task>

<task type="auto">
  <name>Task 3: Create Trend Chart Component</name>
<files>app/frontend/src/components/telemetry/TrendChart.tsx</files>
<action>
Create reusable trend chart component:
- Uses recharts library (already in project)
- Supports multiple metrics on same chart
- Time range selector (1h, 6h, 24h, 7d, 30d, 90d)
- Anomaly highlighting (red dots)
- Hover tooltips with exact values
- Responsive design

Include data aggregation for performance (sample points for large ranges).
  </action>
<verify>
<automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Trend charts render with mock data</done>
</task>

</tasks>

<verification>
- User can view all machines with current telemetry on IoT dashboard
- User can drill down into individual machine telemetry history
- Charts display telemetry trends over time
</verification>

<success_criteria>
- IoT dashboard shows real-time telemetry for fleet
- Machine detail includes telemetry history charts
- User can identify machines with anomalous readings
</success_criteria>

<output>
After completion, create `.planning/phases/08-intelligence-automation/08-04-SUMMARY.md`
</output>
