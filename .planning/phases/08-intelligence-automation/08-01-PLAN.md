---
phase: 08-intelligence-automation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [INTEL-01, INTEL-02]
user_setup: []

must_haves:
  truths:
    - "User sees alerts for machines predicted to fail within configurable days"
    - "User receives notifications when critical alerts are generated"
    - "User can configure alert thresholds (e.g., RUL < 7 days)"
    - "Alerts integrate with work order creation workflow"
    - "Each role receives relevant notifications based on their responsibilities"
  artifacts:
    - path: "app/backend/services/alertes.py"
      provides: "Alert service for creating/reading predictive alerts"
      exports: ["create_alert", "get_active_alerts", "dismiss_alert", "check_and_create_alerts"]
    - path: "app/backend/modules/shared/routes/alerts.py"
      provides: "REST API for alerts CRUD"
      exports: ["GET /alerts", "PATCH /alerts/{id}/dismiss"]
    - path: "app/frontend/src/modules/admin/AlertConfig.tsx"
      provides: "Admin UI for configuring alert thresholds"
      exports: ["AlertConfigForm"]
    - path: "app/frontend/src/modules/shared/AlertsPanel.tsx"
      provides: "User-facing panel showing active alerts"
      exports: ["AlertsPanel"]
    - path: "app/backend/services/notifications.py"  # EXTEND
      provides: "Role-based notification service"
      exports: ["NotificationsService.send_role_notification", "NotificationsService.send_workflow_notification"]
    - path: "app/backend/core/notifications.py"  # EXTEND
      provides: "WebSocket broadcaster for real-time notifications"
  key_links:
    - from: "app/backend/services/alertes.py"
      to: "app/backend/modules/ml/router.py"
      via: "P3 RUL prediction"
      pattern: "get_prediction.*rul"
    - from: "app/backend/services/alertes.py"
      to: "app/backend/services/notifications.py"
      via: "push notification on alert creation"
      pattern: "send_notification.*alert"
    - from: "app/backend/services/alertes.py"
      to: "app/backend/services/ordres_travail.py"
      via: "create work order from alert"
      pattern: "create_work_order_from_alert"
---

<objective>
Implement Predictive Maintenance Alerts system using existing ML P3 (RUL) model, with full workflow integration and role-based notifications.
</objective>

<context>
@.planning/phases/08-intelligence-automation/08-CONTEXT.md
@.planning/phases/08-intelligence-automation/08-RESEARCH.md
@app/backend/modules/ml/router.py
@app/backend/services/notifications.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Notification Service for Role-Based Alerts</name>
  <files>app/backend/services/notifications.py</files>
  <action>
Extend NotificationsService with role-based methods:
- `send_to_role(role: str, notification: dict)` - Send to all users of a role
- `send_workflow_notification(action: str, data: dict)` - Send notifications based on workflow actions
- `get_user_notifications(user_id, unread_only)` - Get user notification history

Workflow actions that trigger notifications:
- WORK_ORDER_CREATED → CHETOP, CHEFTECH (depending on priority)
- WORK_ORDER_ASSIGNED → TECHNICIEN
- WORK_ORDER_COMPLETED → CHEFTECH, CHETOP
- INTERVENTION_APPROVED → TECHNICIEN, CHEFTECH
- INTERVENTION_REJECTED → CHETOP
- ALERT_CREATED → Relevant users based on machine zone/responsibility
- REPORT_GENERATED → Report recipients
- USER_APPROVED → The approved user
  </action>
  <verify>
<automated>python -c "from services.notifications import NotificationsService; print('OK')"</automated>
  </verify>
  <done>Notification service extended</done>
</task>

<task type="auto">
  <name>Task 2: Extend Alert Model Relationships</name>
  <files>app/backend/models/alertes.py</files>
  <action>
Add relationship fields:
- Add `work_order_id` FK to Alert (optional - link alert to created work order)
- Add `is_linked_to_wo` boolean to track if alert generated a work order
- Add `priority` field (for work order priority when created from alert)

Update Machines model relationship if needed.
  </action>
  <verify>
<automated>python -c "from models.alertes import Alert; print(Alert.work_order_id)"</automated>
  </done>
</task>

<task type="auto">
  <name>Task 3: Add "Create Work Order from Alert" Service</name>
  <files>app/backend/services/alertes.py</files>
  <action>
Add method to AlertService:
- `create_work_order_from_alert(alert_id, wo_data)` - Creates work order linked to alert
- `link_existing_work_order(alert_id, work_order_id)` - Links existing WO to alert

Also add to alert API:
- POST /api/v1/alerts/{id}/create-work-order
  </action>
  <verify>
<automated>curl -X POST http://localhost:8000/api/v1/alerts/1/create-work-order -H "Authorization: Bearer $TOKEN"</automated>
  </verify>
  <done>Can create work order from alert</done>
</task>

<task type="auto">
  <name>Task 4: Add Alert Integration with WebSocket</name>
  <files>app/backend/core/notifications.py</files>
  <action>
Ensure WebSocket broadcasts for:
- New alerts (broadcast to all relevant users)
- Alert status changes (dismissed, linked to WO)
- Include machine zone in alert for targeted notifications

Update frontend AlertsPanel to use WebSocket for real-time updates.
  </action>
  <verify>
<automated>Check WebSocket connection in browser devtools</automated>
  </verify>
  <done>Real-time alert updates work</done>
</task>

<task type="auto">
  <name>Task 5: Add Email Notifications for Alerts</name>
  <files>app/backend/services/alertes.py</files>
  <action>
Extend AlertService to send emails:
- If config.notification_email is true, send email to relevant users
- Different email templates based on severity:
  - CRITICAL/HIGH: Immediate email
  - MEDIUM/LOW: Daily digest option

Use existing EmailService from core.email.
  </action>
  <verify>
<automated>Check email sent when alert created with email enabled</automated>
  </verify>
  <done>Email notifications work</done>
</task>

<task type="auto">
  <name>Task 6: Add Alert to Work Order Flow Integration</name>
  <files>app/backend/modules/shared/routes/alerts.py</files>
  <action>
Add endpoint to automatically create work order from alert:
- POST /api/v1/alerts/{id}/create-work-order
- Request body: { priority, type_intervention, description, assigned_to }
- Returns created work order ID

Also update AlertsPanel UI to include "Create Work Order" button.
  </action>
  <verify>
<automated>Test work order creation from alert in UI</automated>
  </verify>
  <done>Can create work order from alert in UI</done>
</task>

<task type="auto">
  <name>Task 7: Add Role-Based Filter to Alerts API</name>
  <files>app/backend/modules/shared/routes/alerts.py</files>
  <action>
Update GET /alerts to filter by user role:
- ADMIN: sees all alerts
- CHETOP: sees alerts for machines in their zone
- CHEFTECH: sees alerts for machines they manage
- TECHNICIEN: sees alerts for assigned machines only

Add machine-zone relationship check.
  </action>
  <verify>
<automated>curl returns different results for different roles</automated>
  </verify>
  <done>Role-based filtering works</done>
</task>

</tasks>

<verification>
- Alert service creates alerts when ML predictions cross thresholds
- User sees alerts in UI with severity colors
- Admin can configure thresholds
- Alerts can be dismissed
- Alerts can create work orders
- Role-based notifications work
- Real-time updates via WebSocket
- Email notifications for critical alerts
</verification>

<success_criteria>
- User with machines can view predictive alerts in UI
- Alerts are automatically generated based on ML RUL predictions
- Alert threshold is configurable by admin
- Alerts integrate with work order creation workflow
- Each role receives relevant notifications
- Real-time updates work
</success_criteria>

<output>
After completion, create `.planning/phases/08-intelligence-automation/08-01-SUMMARY.md`
</output>
