---
phase: 08-intelligence-automation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [INTEL-03, INTEL-04]
user_setup: []

must_haves:
  truths:
    - "User can schedule weekly email reports of asset health"
    - "User can download PDF/Excel reports on demand"
    - "Reports include machine status, upcoming maintenance, and costs"
  artifacts:
    - path: "app/backend/services/rapports.py"
      provides: "Report generation service (extend existing)"
      exports: ["generate_report", "schedule_report", "send_report_email"]
    - path: "app/backend/tasks/rapports.py"
      provides: "Celery tasks for scheduled report generation"
      exports: ["send_weekly_report", "send_daily_digest"]
    - path: "app/frontend/src/modules/admin/ReportScheduler.tsx"
      provides: "Admin UI for scheduling reports"
      exports: ["ReportSchedulerForm"]
    - path: "app/frontend/src/modules/shared/ReportsDownload.tsx"
      provides: "User UI for on-demand report download"
      exports: ["ReportsDownload"]
  key_links:
    - from: "app/backend/tasks/rapports.py"
      to: "app/backend/core/email.py"
      via: "send email with attachment"
      pattern: "send_email.*attachment"
    - from: "app/backend/services/rapports.py"
      to: "app/backend/modules/ml/router.py"
      via: "include ML predictions in report"
      pattern: "get_prediction.*fleet"
---

<objective>
Implement Automated Reporting Engine with scheduled email reports and on-demand PDF/Excel downloads.
</objective>

<context>
@.planning/phases/08-intelligence-automation/08-CONTEXT.md
@.planning/phases/08-intelligence-automation/08-RESEARCH.md
@app/backend/services/rapports.py
@app/backend/core/email.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Report Model and Service</name>
  <files>app/backend/services/rapports.py</files>
  <action>
Extend existing `Rapport` model:
- add report_type: enum (WEEKLY_DIGEST, MONTHLY_SUMMARY, ASSET_HEALTH, MAINTENANCE_COSTS, ML_PREDICTIONS)
- add schedule_config: JSON (frequency, day_of_week, time, recipients)
- add last_sent_at: datetime
- add is_active: bool

Add service functions:
- `generate_pdf_report(report_type, filters)` - Generate HTML and convert to PDF
- `generate_excel_report(report_type, filters)` - Generate Excel with openpyxl
- `send_scheduled_reports()` - Check schedules, generate and send due reports
- `get_report_template(report_type)` - Return report structure
  </action>
  <verify>
<automated>python -c "from app.backend.services.rapports import generate_report; print('OK')"</automated>
  </verify>
  <done>Report service extended with scheduling capabilities</done>
</task>

<task type="auto">
  <name>Task 2: Create Celery Tasks for Scheduled Reports</name>
<files>app/backend/tasks/rapports.py</files>
<action>
Create Celery tasks:
- `send_weekly_report()` - Runs every Monday, generates weekly digest for all recipients
- `send_daily_digest()` - Runs daily, generates daily summary
- `check_and_send_due_reports()` - Checks all active scheduled reports, sends if due

Configure Celery Beat schedule:
- Daily digest: 8:00 AM
- Weekly report: Every Monday 8:00 AM
  </action>
<verify>
<automated>python -c "from app.backend.tasks.rapports import send_weekly_report; print('OK')"</automated>
  </verify>
  <done>Celery tasks registered and scheduled</done>
</task>

<task type="auto">
  <name>Task 3: Create Report API Endpoints</name>
<files>app/backend/modules/admin/routes/reports.py</files>
<action>
Add endpoints:
- GET /api/v1/reports/scheduled — List scheduled reports
- POST /api/v1/reports/scheduled — Create new scheduled report
- PATCH /api/v1/reports/scheduled/{id} — Update schedule
- DELETE /api/v1/reports/scheduled/{id} — Delete schedule
- GET /api/v1/reports/download/{type} — Download PDF/Excel on-demand
- POST /api/v1/reports/test-email — Send test email with sample report
  </action>
<verify>
<automated>curl -X GET http://localhost:8000/api/v1/reports/scheduled -H "Authorization: Bearer $TOKEN"</automated>
  </verify>
  <done>Report API endpoints functional</done>
</task>

<task type="auto">
  <name>Task 4: Create Admin Report Scheduler UI</name>
<files>app/frontend/src/modules/admin/ReportScheduler.tsx</files>
<action>
Create admin page for scheduling reports:
- Select report type (dropdown)
- Set frequency (daily, weekly, monthly)
- Set day/time
- Add recipients (multi-select from users)
- Enable/disable toggle
- View history of sent reports

Use shadcn/ui components (Select, Calendar, MultiSelect).
  </action>
<verify>
<automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Report scheduler UI renders correctly</done>
</task>

<task type="auto">
  <name>Task 5: Create Reports Download UI</name>
<files>app/frontend/src/modules/shared/ReportsDownload.tsx</files>
<action>
Create user-facing reports page:
- List available report types with descriptions
- Filter options (date range, machine, zone)
- Download buttons for PDF and Excel
- "Send to my email" button
- History of previously generated reports

Include quick stats: total machines, active alerts, upcoming maintenance count.
  </action>
<verify>
<automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Reports download page functional</done>
</task>

</tasks>

<verification>
- User can schedule weekly report and receives email
- User can download PDF/Excel on-demand
- Reports include ML predictions and machine status
</verification>

<success_criteria>
- Admin can schedule recurring reports
- Users can download asset health reports in PDF/Excel format
- Scheduled reports are delivered via email
</success_criteria>

<output>
After completion, create `.planning/phases/08-intelligence-automation/08-02-SUMMARY.md`
</output>
