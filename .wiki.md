# Project Summary
The Asset Management System is a comprehensive web application designed to optimize the management of industrial assets, work orders, and maintenance schedules. It enhances operational efficiency through user management, machine tracking, work order processing, planning, archiving, and reporting, ultimately streamlining asset management processes for businesses.

# Project Module Description
- **User Management**: Handles user roles, authentication, and password management.
- **Machine Management**: Manages machine details, statuses, and maintenance schedules.
- **Work Orders**: Facilitates creation, editing, and tracking of work orders related to machines.
- **Planning**: Supports scheduling and planning of maintenance and work orders.
- **Interventions**: Tracks and documents maintenance interventions with full CRUD functionality.
- **Reports**: Generates and manages operational and maintenance reports with full CRUD capabilities.
- **Dashboard**: Provides an overview of key metrics and statuses across the system.
- **Technician Interface**: A dedicated interface for technicians with role-specific features and permissions.

# Directory Tree
```
/workspace/app/
├── backend/                # Backend services and API
│   ├── models/             # ORM models
│   ├── routers/            # API routes
│   ├── services/           # Business logic
│   └── main.py             # Entry point for the backend
└── frontend/               # Frontend application
    ├── src/                # Source files for React components
    │   ├── pages/          # Application pages (Interventions, Planning, Reports, Technician specific pages)
    │   ├── lib/            # Type definitions and API client
    ├── public/             # Static files
    └── index.html          # Main HTML file
```

# File Description Inventory
- **backend/**: Contains all backend logic, including models, routes, and services for handling data and business logic.
- **frontend/**: Contains all frontend files including React components, styles, and public assets.
- **README.md**: Documentation for project setup and usage.
- **requirements.txt**: Lists dependencies for backend and frontend.
- **TECHNICIAN_IMPLEMENTATION_SUMMARY.md**: Documentation summarizing the implementation of the Technician role.

# Technology Stack
- **Backend**: Python, FastAPI, SQLAlchemy
- **Frontend**: React, TypeScript, Tailwind CSS, Shadcn-ui
- **Database**: PostgreSQL
- **Authentication**: Atoms Backend for user management and authentication

# Usage
1. **Install Dependencies**:
   - For the backend, navigate to the `/backend` directory and run `pip install -r requirements.txt`.
   - For the frontend, navigate to the `/frontend` directory and run `pnpm install`.
   
2. **Build**: 
   - For the frontend, run `pnpm run build` to create a production build.
   
3. **Run**:
   - Start the backend service by executing `uvicorn main:app --reload` in the backend directory.
   - Start the frontend development server by running `pnpm run dev` in the frontend directory.
