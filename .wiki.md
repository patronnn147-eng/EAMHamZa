# Project Summary
The Asset Management System is an advanced web application focused on optimizing the management of industrial assets, work orders, and maintenance schedules. By utilizing efficient user management, machine tracking, and work order processing, it enhances operational efficiency for businesses, ultimately streamlining their asset management processes.

# Project Module Description
- **User Management**: Manages user roles, authentication, and password handling with a focus on JWT-based authentication.
- **Machine Management**: Oversees machine details, statuses, and maintenance schedules.
- **Work Orders**: Facilitates the creation, editing, and tracking of work orders related to machines.
- **Planning**: Supports the scheduling and planning of maintenance and work orders.
- **Interventions**: Tracks and documents maintenance interventions with full CRUD functionality.
- **Reports**: Generates and manages operational and maintenance reports.
- **Dashboard**: Provides an overview of key metrics and statuses across the system.
- **Technician Interface**: A dedicated interface for technicians featuring role-specific capabilities.

# Directory Tree
```
/workspace/app/
├── backend/                # Backend services and API
│   ├── core/               # Core authentication logic
│   ├── dependencies/        # Dependency injection and authentication
│   ├── models/             # ORM models
│   ├── routers/            # API routes
│   ├── schemas/            # Data validation schemas
│   ├── services/           # Business logic
│   └── main.py             # Entry point for the backend
└── frontend/               # Frontend application
    ├── src/                # Source files for React components
    │   ├── pages/          # Application pages (Interventions, Planning, Reports, Technician specific pages)
    │   ├── lib/            # Type definitions and API client
    ├── public/             # Static files
    └── index.html          # Main HTML file
```

# File Description Inventory
- **backend/**: Contains all backend logic, including models, routes, services, and authentication.
- **frontend/**: Contains all frontend files, including React components, styles, and public assets.
- **README.md**: Documentation for project setup and usage.
- **requirements.txt**: Lists dependencies for backend and frontend, including bcrypt for password hashing.
- **JWT_AUTHENTICATION_MIGRATION.md**: Documentation summarizing the migration from OIDC to JWT authentication.

# Technology Stack
- **Backend**: Python, FastAPI, SQLAlchemy
- **Frontend**: React, TypeScript, Tailwind CSS, Shadcn-ui
- **Database**: PostgreSQL
- **Authentication**: JWT-based authentication using passlib for password hashing

# Usage
1. **Install Dependencies**:
   - For the backend, navigate to the `/backend` directory and run `pip install -r requirements.txt`.
   - For the frontend, navigate to the `/frontend` directory and run `pnpm install`.
   
2. **Build**: 
   - For the frontend, run `pnpm run build` to create a production build.
   
3. **Run**:
   - Start the backend service by executing `uvicorn main:app --reload` in the backend directory.
   - Start the frontend development server by running `pnpm run dev` in the frontend directory.
